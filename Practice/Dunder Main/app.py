import alphabet


word_list = ["hamster", "turtle", "cat", "bird"]
last_names = ["Rodriguez", "Lee", "Yakinian", "Amendola"]

# alphabet.func_name(word_list)
alphabet.sort_list(word_list)

# alphabet.func_name(last_names)
alphabet.sort_list(last_names)
